# Changesets

This folder is used by `@changesets/cli` to track user-facing package changes
and produce version bumps/changelog entries for monorepo releases.

Common commands:

- `yarn changeset` — create a new changeset entry
- `yarn changeset:version` — apply version bumps and changelog updates
